class CardInfo {
    id: string;
    title: string;
    price: string | number;
    address: string;
    images: string[];
    createdAt: string;
    seen: boolean;
    about: string;
}

export default CardInfo;